# GC-MS Metabolic Profiling Tool — runtime requirements
# Tested on Python 3.10, 3.11, 3.12, 3.13 / Windows, macOS, Linux.

# --- Core data stack ---------------------------------------------------------
pandas>=2.0,<3.0
numpy>=1.24,<3.0
scipy>=1.10,<2.0

# --- Statistics and ordination ----------------------------------------------
scikit-learn>=1.3,<2.0

# --- Plotting ----------------------------------------------------------------
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0

# --- PDF parsing -------------------------------------------------------------
pdfplumber>=0.11,<1.0

# --- Word report writer ------------------------------------------------------
python-docx>=1.0,<2.0

# --- HTTP client for PubChem PUG REST enrichment ----------------------------
requests>=2.31,<3.0

# --- Optional: faster string deduplication (improves synonym detection) -----
RapidFuzz>=3.0,<4.0
